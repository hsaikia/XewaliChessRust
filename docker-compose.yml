services:
  lichess-bot:
    image: ghcr.io/hsaikia/xewalichessrust:latest
    container_name: xewali-bot
    restart: unless-stopped
    env_file: .env
    deploy:
      resources:
        limits:
          memory: 512M
          cpus: "1.0"
        reservations:
          memory: 256M
